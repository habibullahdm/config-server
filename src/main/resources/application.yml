spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          skipSslValidation: true
          force-pull: true
          search-paths: dev, prod
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}

server:
  port: 8888
