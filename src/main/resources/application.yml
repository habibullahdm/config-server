spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}
          skipSslValidation: true
          force-pull: true
          search-paths: dev, prod

server:
  port: ${PORT:8888}
